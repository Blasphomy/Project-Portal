# User Service - Docker Profile

# Database (External Supabase PostgreSQL or default local)
spring.r2dbc.url=${DATABASE_URL:r2dbc:postgresql://localhost:5432/learning_db}

# Security
token.signing.key=${JWT_SECRET}

# Container logging
logging.file.name=/app/logs/user-service.log
logging.level.com.project.user=INFO
